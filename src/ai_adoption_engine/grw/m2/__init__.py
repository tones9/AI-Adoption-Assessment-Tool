"""Narrow, approval-gated GRW M2 reassessment lifecycle."""

# Keep this namespace import-light: persistence imports M2 contracts before the
# service layer, and the service itself depends on the persistence adapter.
